# 2026-09-10 本地验证记录

## 环境

Windows 11 x64（10.0.26200），.NET SDK 10.0.400，已安装 .NET Framework 4.8.1。目标程序为 .NET Framework 4.8，分别生成 x86/x64 可执行文件。

## 实际验证

- x86 / x64 构建均通过：0 warnings、0 errors。
- 两种进程分别运行 32 项离线检查，全部通过。覆盖绑定参数、注入输入、多语句与未支持 SQL 拒绝、XML 编码、外部实体拒绝、来源唯一性、目录更新/备份、密码加密、导出原子性和文本/数值精度。
- 两种进程分别运行实际 WinForms 窗口冒烟检查：模拟查询、条件/数据源生成、结果筛选（含括号）、排序、Excel 导出、收藏、目录搜索、重复查询、连接配置窗口渲染，均通过。
- 只读扫描 `D:\系统知识库\00_Inbox\yljhis\reports`：识别查询定义 **1320**；跳过非查询 XML **1834**；解析错误 **0**；符合当前保守静态适配规则 **356**；待适配 **964**。
- 356 是「可试查候选」，不是已跑通或已核对报表数量。964 份保留搜索入口，交叉/映射/复杂控件及未支持 SQL 语法等原因可在界面查看。
- 独立使用 Python openpyxl 重新打开两种架构生成的测试/演示 xlsx；验证工作表、前导零编号、金额、长数字文本及公式样式字符串保持文本。
- NuGet 直接与间接依赖漏洞查询在本次检查时未报告漏洞；不是安全性证明。
- 人工查看 WinForms 截图，检查主界面与连接对话框布局。源码空白检查未发现行尾空白。
- 当前工作目录原本不是 Git 仓库；新建独立 ReportDesk 目录，没有提交/推送或现有项目 diff。知识库仅被读取，没有修改其文件。

## 问题与修正

- 初次构建发现 Oracle 驱动的 `Enlist` 属性需要字符串；按当前包类型修正后构建通过。
- 测试工程缺少 Compression 引用，补齐后通过。
- 初次全 XML 严格解析遇到旧版式中的非法控制字符；调整为先识别根节点，仅解析本版支持的查询定义。原文件不修改，复测解析错误为 0。

## 未验证

Windows 10 真正 32 位系统、Windows 10 64 位系统、Oracle 19c 连接/执行/取消/超时/只读事务、真实报表结果与 HIS 对齐、Excel/WPS 实际应用打开，均为 **NOT RUN**。没有使用真实数据库凭据，也没有发起数据库连接。

证据目录：`artifacts/verification/x86`、`artifacts/verification/x64`；分发包和哈希清单：`artifacts/packages`。后续按 Acceptance.md 在内网逐项验收。
